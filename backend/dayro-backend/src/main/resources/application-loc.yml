spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dayro
    username: dayro
    password: dayro
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  ai:
    openai:
      api-key: local-dev-placeholder