# 2026-08 의도 아카이브

완료된 8월 의도(sdd/tasks)를 압축한다. 원본 전문은 삭제됐고 git이 보관한다.
과거 결정을 찾을 때는 이 파일에서 `domain:` 태그를 grep 한다.

전문 복구(각 블록의 `원본: <ref> <path>` 기준):
```
git show <ref>:frontend/.agents/intent/<path>
# 예) git show 180660b:frontend/.agents/intent/sdd/2026-08-07-course-save-api.md
```

참고: `.agents/reports/runs/*`, `.agents/reports/validation/*`의 과거 로그는 압축 전 intent 경로(`intent/sdd|tasks/<날짜>-<slug>.md`)를 그대로 참조한다. 이는 당시 기록이므로 수정하지 않으며, 해당 원본은 이 파일의 같은 `task_id` 블록 + git으로 추적한다.

블록 스키마는 `.agents/intent/archive/README.md`를 따른다.

---

## 2026-08-07-course-save-api
- domain: course-map, saved
- 의도: 코스 저장/저장코스 수정을 실제 백엔드 API에 연동.
- 결정: 저장/수정은 상호작용 이후 요청 → server+BFF가 외부 호출·인증쿠키·transport 소유, 브라우저는 BFF `/api/courses`(`/:id`)만 호출. 저장 orchestration은 `course-map/hooks/useCourseMapScreen`, 수정은 `saved/hooks/useSavedCourseDetailScreen`가 소유. 시트/상세는 입력·CTA 조합만.
- 근거/제약: 저장 payload 직렬화는 각 feature 내부 유지, shared 승격 안 함(공용화 근거 없음). 추천 응답 `photoUrl`을 저장 스냅샷 계약에 포함. 비로그인 저장은 로그인 진입으로 연결돼야 함.
- 검증: approved_with_notes (reports/validation/2026-08-07-course-save-api.md, 2026-08-07-saved-course-boundary-validation.md)
- superseded_by: -
- 원본: 180660b sdd/2026-08-07-course-save-api.md, tasks/2026-08-07-course-save-api.md

## 2026-08-10-global-hamburger-navbar
- domain: common (auth)
- 의도: 전역 햄버거/계정 메뉴를 화면 전반 navbar에 확장.
- 결정: `features/auth`가 `NavBar + AccountMenu` 조합 래퍼를 공개하고 widget은 shared `NavBar` 대신 래퍼 사용. 열림 상태·세션 로직은 `AccountMenu` client component가 소유, 화면 위젯은 server component 유지.
- 근거/제약: `shared/ui`는 `features/auth`를 참조 불가 → shared 수정 최소화, feature 래퍼로 해결. 각 화면의 `showBack/backHref/center` 계약 유지, `right`만 auth 메뉴로 고정.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 180660b sdd/2026-08-10-global-hamburger-navbar.md, tasks/2026-08-10-global-hamburger-navbar.md

## 2026-08-10-situation-popular-keywords
- domain: course-situation
- 의도: 지역 검색 step에 인기 검색어 칩 UX 추가.
- 결정: 인기 검색어는 region step 첫 진입 기준 데이터 → 서버 preload 우선, 외부 `GET /api/regions/popular-keywords`는 shared server 계층 소유. `situation` feature가 칩 view-model/hook 공개, region 상태·전이는 `hooks/useRegionStep`가 소유, `RegionAreaSearch`는 렌더만.
- 근거/제약: 칩은 canonical `districtId`+label+구라벨 view-model로 정규화, 클릭은 같은 정규화 경로로 즉시 선택. 인기 검색어는 빈 검색어 상태에서만 노출(이미 선택된 값 오해 방지).
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 180660b sdd/2026-08-10-situation-popular-keywords.md, tasks/2026-08-10-situation-popular-keywords.md

## 2026-08-11-course-loading-chosung-quiz
- domain: course-situation (loading-quiz)
- 의도: 코스 생성 로딩 화면에 초성퀴즈 미니게임 추가.
- 결정: 초성퀴즈는 순수 client 조각(`features/loading-quiz`, server 엔트리 없음). 배럴은 `ChosungQuizCard`,`useChosungQuiz`+타입만 export. 상태기계는 `useChosungQuiz`, 로딩 완료(ready)+결과 이동은 기존 `useSituationFlowController`가 소유. CTA는 타이머가 아니라 **실제 코스 생성 완료**(`phase!=="pending"`) 기준 노출.
- 근거/제약: **결과/지도 데이터 소스는 서버 page(`getCourseNewPageData`) 유지, sessionStorage로 승격 금지** — 과거 sessionStorage 소스화 시 지도 미표시 버그가 있었음. 로딩의 client 생성은 같은 요청을 미리 호출해 캐시 워밍 용도. 초성은 유니코드 분해 자동생성, 정답은 trim 후 완전일치.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: e8396b5 sdd/2026-08-11-course-loading-chosung-quiz.md, tasks/2026-08-11-course-loading-chosung-quiz.md

## 2026-08-14-agentic-engineering-architecture
- domain: agents-ops
- 의도: `.agents` 운영 문서를 Agentic Engineering 4축으로 재정렬.
- 결정: 최상위=Agentic Engineering, 하위 4축=Intent/Context/Harness/Orchestration. `context/README.md` 신설. PRD/SDD/Execution Spec/AC는 Intent 소관, Spec Kit은 객체 아닌 통칭. Harness는 loop 이상(tool/env, eval, observability, guardrails)으로 확장.
- 근거/제약: 기존 파일 경로를 최대한 재사용해 문서 drift 최소화. 역할 문서가 새 축을 참조하지 않으면 옛 분류가 계속 쓰임 → role 문서 상호참조 보강 필요.
- 검증: approved (reports/validation/2026-08-14-agentic-engineering-architecture.md)
- superseded_by: -
- 원본: acf17a6 sdd/2026-08-14-agentic-engineering-architecture.md, tasks/2026-08-14-agentic-engineering-architecture.md

## 2026-08-14-course-map-route-deeplink
- domain: course-map
- 의도: 확정 코스 지도에서 네이버 웹 길찾기로 연결.
- 결정: 네이버지도 단일(카카오는 표시 전용). URL 빌더는 브라우저 비의존 순수함수 `features/course-map/lib/route-deeplink.ts`(슬라이스 내부 lib, 배럴 승격 안 함), `window.open`은 `useCourseMapScreen`가 소유. BFF/서버 호출 없음.
- 근거/제약: 좌표 있는 장소만 순서대로(첫=출발s, 마지막=도착d, 중간=경유v1..vN), **좌표 유효 2개 미만이면 null→토스트**. 코스 간 이동은 **항상 walk**(사용자 transport 미사용). 팝업 정책 탓 클릭 제스처 내 동기 오픈 필수. **`domain/course-map.md`의 Out-of-Scope를 외부 내비 연동 허용으로 갱신함**(사용자 확정).
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: f88b966 sdd/2026-08-14-course-map-route-deeplink.md, tasks/2026-08-14-course-map-route-deeplink.md

## 2026-08-18-course-reroll-retry-api
- domain: course, course-result, course-situation
- 의도: "다른 코스 보기"를 백엔드 retry API에 반영.
- 결정: 최초=`POST /api/situations`, 다른코스=`POST /api/situations/{requestId}/retry`. 서버가 `requestId`,`remainingRetries`를 page data로 내림. reroll은 `requestId+retryKey` loading URL로 push→loading의 client generation이 retry 호출→완료 후 result URL은 `requestId`만 유지(`retryKey` 제거). URL 직렬화/복원은 `features/situation/model/url-state.ts`, 라우팅 정책은 `useSituationFlowController`.
- 근거/제약: course step은 URL snapshot(`PlaceCandidate[]`)이 있으면 재추천 API 재호출 없이 복원, 없으면 `selectedPlaceIds` fallback(구 URL 호환). loading 완료 전 새로고침 시 retry 재실행 위험 → 완료 후에만 result 복귀. snapshot 탓 URL 길이 증가.
- 검증: (후속 검증 태스크 course-reroll-api-validation 참조)
- superseded_by: -
- 원본: 454a3eb sdd/2026-08-18-course-reroll-retry-api.md, tasks/2026-08-18-course-reroll-retry-api.md

## 2026-08-18-course-reroll-api-validation
- domain: course-result
- 의도: reroll retry API 반영분에 대한 검증 태스크.
- 결정: retry endpoint path가 OpenAPI/BFF/server transport/tests에 일관 반영, `useSituationFlowController` 외 UI/widget에 URL 정책 누출 금지, snapshot 존재 시 `submitSituation` 재호출 없이 복원.
- 근거/제약: rejected 판정 — 위 일관성/경계 기준 미충족분이 있었음. 관련 후속은 이후 course/situation 작업으로 이어짐.
- 검증: rejected (reports/validation/2026-08-18-course-reroll-api-validation.md)
- superseded_by: -
- 원본: 454a3eb tasks/2026-08-18-course-reroll-api-validation.md

## 2026-08-23-context-matrix-replay-probe
- domain: agents-ops
- 의도: context selection matrix를 실제 과거 사례로 replay 검증.
- 결정: matrix canonical source는 수정하지 않고 replay 결과만 reports에 기록. 사례별 task type/trigger/required docs/actual docs/판정을 표로 정리. same-thread validation-only probe.
- 근거/제약: 기존 run log의 문서 목록이 부실하면 일부 수동 해석 필요. observability 5건 기준 충족 확인.
- 검증: approved (reports/validation/2026-08-23-context-matrix-replay-probe.md)
- superseded_by: -
- 원본: fd13d11 sdd/2026-08-23-context-matrix-replay-probe.md, tasks/2026-08-23-context-matrix-replay-probe.md

## 2026-08-23-context-observability-round2
- domain: agents-ops
- 의도: context loading matrix + observability summary 2차 보강.
- 결정: matrix canonical=`.agents/context/README.md`, observability summary 저장=`.agents/reports/observability/`. 새 디렉터리 추가 없음. matrix는 추상 정책이 아닌 작업유형별 선택표로 작성, summary는 실제 run log만 근거.
- 근거/제약: 표가 과대해지면 context loading이 오히려 무거워짐(크기 경계 주의). run log 수가 적어 질적 해석 비중 큼.
- 검증: approved (reports/validation/2026-08-23-context-observability-round2.md)
- superseded_by: -
- 원본: fd13d11 sdd/2026-08-23-context-observability-round2.md, tasks/2026-08-23-context-observability-round2.md

## 2026-08-23-harness-quick-loop-evidence
- domain: agents-ops
- 의도: Quick Loop / evidence bundle 운영 규칙 보강.
- 결정: `Quick Loop`,`Full Loop`,`evidence bundle` 기준 원본=`.agents/harness/README.md`, report/run log 기록 책임=`.agents/reports/README.md` 계층. 새 상위 디렉터리/artifact 타입 없음. Quick Loop=최소 intent/self-check/evidence 3요소 계약.
- 근거/제약: Quick Loop이 템플릿 수준으로 안 닫히면 기존 설명형과 차이 미미. evidence bundle 과세분화 시 유지비 증가. Full Loop 최소 evidence 기준 유지 필요.
- 검증: approved_with_notes (reports/validation/2026-08-23-harness-quick-loop-evidence.md)
- superseded_by: -
- 원본: fd13d11 sdd/2026-08-23-harness-quick-loop-evidence.md, tasks/2026-08-23-harness-quick-loop-evidence.md

## 2026-08-23-quick-loop-probe-suite
- domain: faq, home
- 의도: Quick Loop 실검증용 소규모 logic probe 4종(FAQ validation/form, Home subtitle, toast lifecycle).
- 결정: 모든 probe는 client/pure logic 한정, route handler·BFF·server fetch 경계 미수정. 기존 public surface(`ContactForm`,`HomeEntryCard`,`useToast`) 유지. 상위 task 아래 4 micro-task를 sequential DAG로.
- 근거/제약: FAQ validation은 domain invariant를 model 수준에서 직접 보장, UI는 domain limit를 native affordance로 반영. toast는 timeout lifecycle 테스트 가능화. shared toast probe는 다수 소비처 걸쳐 회귀 테스트 중요, FAQ hook 테스트에 next/navigation mock 필요할 수 있음.
- 검증: approved_with_notes (reports/validation/2026-08-23-quick-loop-probe-suite.md)
- superseded_by: -
- 원본: fd13d11 sdd/2026-08-23-quick-loop-probe-suite.md, tasks/2026-08-23-quick-loop-probe-suite.md

## 2026-08-25-auth-withdraw-api
- domain: withdraw, mypage
- 의도: 회원탈퇴를 실제 백엔드 API에 연동.
- 결정: 탈퇴는 확인 이후 상호작용 요청 → 외부 호출·인증헤더는 `server-auth-client`+BFF, 브라우저는 BFF `/api/auth/withdraw`만. `features/auth`는 client-safe `requestWithdraw`,`useWithdraw`,`WithdrawReasonForm`만 공개. 사유/모달 open state는 폼, 요청·pending/error·성공 후 라우팅은 `useWithdraw`.
- 근거/제약: **BFF는 성공 시에만 auth 쿠키 제거**(실패 시 쿠키 유지 — 백엔드와 회원상태 어긋남 방지). 성공 후 보호경로 캐시 제거 위해 `router.refresh()` 필요. 탈퇴 사유는 서버 계약 미포함 → UI 로컬 상태.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 21d929d sdd/2026-08-25-auth-withdraw-api.md, tasks/2026-08-25-auth-withdraw-api.md

## 2026-08-25-client-logic-separation-followup
- domain: auth, faq, course-map, course-situation
- 의도: client 로직 분리 후속 정리(상태/effect/이벤트 정책을 ui→hook 이동).
- 결정: `ui`는 JSX 조합·접근성 연결만, 상태·effect·이벤트정책·오버레이·브라우저이벤트/스크롤은 대응 훅으로. 매핑: `AccountMenu→useAccountMenu`, `FaqAccordion→useFaqAccordion`, `CoursePlaceList row→useCoursePlaceRow`, `SituationSelectionTicker→useSituationSelectionTicker`, `WheelColumn→useWheelColumn`. 서버/BFF 경계·API 경로 변경 없음, shared 승격 없음.
- 근거/제약: drag/scroll/animation 훅 분리 중 상호작용 회귀 위험, 접근성 이름/열림 상태 불변 유지. 검증에 e2e 포함.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: f9e4051 sdd/2026-08-25-client-logic-separation-followup.md, tasks/2026-08-25-client-logic-separation-followup.md

## 2026-08-27-mypage-logout-toast
- domain: login, mypage
- 의도: 로그아웃 후 로그인 화면에 안내 토스트 노출.
- 결정: 로그아웃 BFF 유지, 성공 후 라우팅 query는 client hook 생성, server page가 notice key를 widget에 전달, 표시·자동소멸은 `useLoginNoticeToast`. `features/auth/index.ts`에 client-safe `LoginNoticeToast`+notice helper만 추가. 로그아웃 후속 UX=`useLogout`. 성공 경로 `GET /login?notice=logged_out`.
- 근거/제약: notice key→문구 매핑은 `model/oauth.ts` helper(widget은 message만 소비). **raw message를 query로 열지 말 것**(성공 문구 주입 경로 방지). mount 시 토스트 반복 노출 방지, 오류 message와 notice message 혼선 방지.
- 검증: approved (reports/validation/2026-08-27-mypage-logout-toast.md)
- superseded_by: -
- 원본: 7c25e31 sdd/2026-08-27-mypage-logout-toast.md, tasks/2026-08-27-mypage-logout-toast.md

## 2026-08-28-home-image-delivery-and-ttfb
- domain: home
- 의도: 홈 카드 이미지 전송 최적화 + TTFB 점검.
- 결정: 홈 카드 WebP 2장을 표시크기·알파 유지하며 재압축. 이미지 압축은 resource load duration 개선(element render delay 개선 아님). 홈 `/`은 서버 API 대기 없음·인증 proxy matcher 비포함 → production TTFB 양호하면 추측성 변경 안 함. 기존 Web Vitals 콘솔 로깅 유지.
- 근거/제약: **TTFB 판정은 dev 서버가 아닌 production build document 요청 기준**(dev 컴파일 시간을 병목 오판 금지). 과압축은 시각 품질 훼손 위험.
- 검증: approved (reports/validation/2026-08-28-home-image-delivery-and-ttfb.md)
- superseded_by: -
- 원본: 462977a sdd/2026-08-28-home-image-delivery-and-ttfb.md, tasks/2026-08-28-home-image-delivery-and-ttfb.md

## 2026-08-28-home-lcp-image-tuning
- domain: home
- 의도: 홈 LCP(첫 카드 이미지) 렌더 지연 개선.
- 결정: 홈 server component 경계 유지. `HomeEntryCard`의 `imagePriority` API는 유지하되 내부를 Next 16 preload 경로로. 첫 카드 이미지에 `preload`+`loading="eager"`+`fetchPriority="high"`+`decoding="sync"`. hot path에서 `@/shared/ui` 배럴 import 회피.
- 근거/제약: LCP target=홈 첫 카드 `img`(TTFB 낮고 resource load/element render delay 높음). **eager/preload 과다 확장 시 다른 리소스 우선순위 훼손** → 첫 카드에만 한정(과최적화 금지).
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 9aec0b4 sdd/2026-08-28-home-lcp-image-tuning.md, tasks/2026-08-28-home-lcp-image-tuning.md

## 2026-08-28-home-web-vitals-logging
- domain: home
- 의도: 홈에 Web Vitals 계측 로깅 추가.
- 결정: 홈 server component 유지, 수집은 홈에만 마운트되는 작은 client component(`widgets/home/HomeWebVitalsLogger`). `useReportWebVitals`로 CLS/FCP/INP/LCP/TTFB 로그, attribution은 `next.config.ts`의 `experimental.webVitalsAttribution`. **dev 환경 콘솔 출력만**(production 동작·analytics 전송은 범위 밖).
- 근거/제약: dev 로그는 실운영 수치와 차이 있음. 홈 외 화면엔 logger 미마운트.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 9aec0b4 sdd/2026-08-28-home-web-vitals-logging.md, tasks/2026-08-28-home-web-vitals-logging.md

## 2026-08-28-performance-followup-4-fixes
- domain: home, course, login
- 의도: 성능 후속 4개 수정(중복 추천요청 제거, 메뉴 세션 지연조회, 배럴 import 축소, 이미지).
- 결정: `/course/new` 중복 추천은 server page+client loading/result bridge 함께 조정(결과 후보를 loading client 응답의 URL snapshot으로 넘겨 server 재요청 회피). 홈 계정 메뉴 세션 조회는 최초 마운트가 아니라 **메뉴 열림 시점**(`useAccountMenu`). 기존 feature public API 유지, 번들 축소용 direct import는 hot path 위주. PlaceCard 이미지는 `next/image`.
- 근거/제약: 결과 후보 snapshot query 비대 가능, 메뉴 첫 open 시 짧은 로딩 발생 가능, direct import 경로 오타 시 빌드 실패 위험.
- 검증: 리포트 없음 (완료·커밋됨. 관련 2026-08-28-unit-and-e2e-suite=approved_with_notes)
- superseded_by: -
- 원본: cda39b5 sdd/2026-08-28-performance-followup-4-fixes.md, tasks/2026-08-28-performance-followup-4-fixes.md

## 2026-08-28-saved-course-directions-share
- domain: saved, course-map
- 의도: 저장 코스 상세에 길안내 공유 추가.
- 결정: 공유 대상 계산·브라우저 공유 API는 client hook(`features/saved/hooks/useSavedCourseDetailScreen`), widget은 버튼 렌더·핸들러 바인딩만. 기존 `features/course-map/lib/route-deeplink.ts` 재사용(신규 shared 승격 없음). saved detail 현재 정렬 `places→points`를 딥링크 입력으로.
- 근거/제약: 공유 URL은 `appUrl` 아닌 `webFallbackUrl` 우선. 좌표 2개 미만이면 링크 생성 불가. `navigator.share` 브라우저 지원 차이 → clipboard 폴백. 버튼 문구 변경 시 e2e 선택자 동반 갱신.
- 검증: 리포트 없음 (완료·커밋됨)
- superseded_by: -
- 원본: 31f54e9 sdd/2026-08-28-saved-course-directions-share.md, tasks/2026-08-28-saved-course-directions-share.md

## 2026-08-29-runtime-role-ownership
- domain: agents-ops
- 의도: Claude/Codex runtime 역할 소유권을 실제 사용 방식에 맞게 문서로 조정.
- 결정: Claude = UI/UX·디자인 시스템 설계/구현/수정 owner. Codex = 비즈니스 로직 설계/구현·코드리뷰·단위 테스트·검증 owner. task마다 coordinator 1명만. e2e는 사용자 지시 전용(Codex 기본 테스트 범위 제외).
- 근거/제약: active policy에 "Claude가 전체 feature implementation owner" 문구를 두지 않는다. UI 구현에 비즈니스 로직이 섞이면 Codex gate로 분리. 단위/e2e 경계를 명확히 안 하면 에이전트가 사용자 지시 없이 e2e 실행 위험. MCP-connected dual-runtime + single-coordinator 규칙은 유지.
- 검증: approved_with_notes (reports/validation/2026-08-29-runtime-role-ownership.md)
- superseded_by: -
- 원본: cf5a200 sdd/2026-08-29-runtime-role-ownership.md, tasks/2026-08-29-runtime-role-ownership.md

## 2026-08-29-multi-agent-orchestration
- domain: agents-ops
- 의도: Claude/Codex를 role switching이 아닌 MCP-connected dual-runtime 멀티 에이전트로 운영하도록 정책 수정.
- 결정: active policy = MCP-connected dual-runtime 기본 모델. same-thread role switching은 small task/단일 런타임 fallback. task마다 coordinator 1명. Codex = architecture/validation/review gate owner. Claude↔Codex MCP 상호 호출 가능하되 recursive call chain 금지. 상세 규칙은 `.agents/orchestration/README.md`에 집중.
- 근거/제약: coordinator를 하나로 고정하면 양방향 MCP와 충돌 가능 → fallback 유지. 정책을 여러 문서에 중복 금지(canonical source). 과거 report의 same-thread 표현은 과거 evidence로만 본다(소급 수정 X).
- 검증: approved_with_notes (reports/validation/2026-08-29-multi-agent-orchestration.md — 최초 blocked, external_blocker(Codex→Claude MCP 실행) 해소 후 갱신. 서브에이전트 노출은 세션 리로드 후 재검증 예정)
- superseded_by: -
- 원본: cf5a200 sdd/2026-08-29-multi-agent-orchestration.md, tasks/2026-08-29-multi-agent-orchestration.md

## 2026-08-29-intent-lifecycle-active-archive
- domain: agents-ops
- 의도: 의도 파일 누적으로 인한 용량·문맥 비용을 줄이기 위해 hot(최근/진행)/cold(월별 압축) 생명주기를 도입.
- 결정: 기본 의도 로딩은 active/index.md·active/current.md 우선. 작은 작업은 파일 생성 없이 대화 내 실행명세 허용. 완료·후속 없는 의도는 archive/YYYY-MM.md 한 블록으로 접고 원본 삭제(커밋된 파일만, 배치+승인, 전문은 git 보관 = A안). hot(sdd/,tasks/)엔 최근/진행 중만 유지.
- 근거/제약: 색인 없이 이동하면 과거 참조가 깨질 수 있어 active 색인 우선 + archive 블록에 원본 git-ref 보존. 진행 중·차단 의도는 아카이브 금지. reports/runs·validation의 과거 intent 경로 참조는 당시 기록이라 소급 수정하지 않음.
- 검증: approved_with_notes (reports/validation/2026-08-29-intent-lifecycle-active-archive.md)
- superseded_by: -
- 원본: cf5a200 sdd/2026-08-29-intent-lifecycle-active-archive.md, 7f7a508 tasks/2026-08-29-intent-lifecycle-active-archive.md

## 2026-08-30-situation-step-guide
- domain: course-situation
- 의도: 코스 만들기 첫 진입 시 진행 순서(시간→장소→목적)를 안내 토스트로 노출.
- 결정: 표시 UI는 features/situation(디자인시스템 Toast 재사용, 순서는 SITUATION_STEPS 단일 소스에서 파생, 상태 없는 프레젠테이션). 노출 조건은 widgets 컨트롤러 훅(첫 스텝 신규 진입만·매 진입·2초 자동 소멸·SSR/hydration 안전). 표시=UI 계층, 동작=로직 계층 경계 유지.
- 근거/제약: 순서를 단일 소스에서 파생 → 단계 구성 변경 시 안내 문구 자동 추종. persistence 없음(매 진입 노출). 딥링크·복귀 진입은 미노출.
- 검증: approved_with_notes (reports/validation/2026-08-30-situation-step-guide-always-show.md)
- superseded_by: -
- 원본: d1e88c8 (inline 실행명세 — sdd/tasks 파일 없음)

## 2026-08-31-saved-empty
- domain: saved
- 의도: 저장한 코스가 없을 때 코스 만들기로 유도하는 빈 상태 CTA를 제공.
- 결정: 빈 상태 UI는 features/saved(디자인시스템 공용 Button(primary)·Illustration 재사용, 정적 문구는 shared/static/saved로 분리). widgets/saved는 courses가 비면 SavedEmpty를 렌더. CTA는 /course/new로 이동. 표시=UI 계층, 비즈니스 로직 없음(정적 링크).
- 근거/제약: 공용 컴포넌트 재사용으로 새 디자인 없음. e2e에서 저장 0개 상태는 mock 전용 토큰(mock-empty-access-token) 분기로 재현.
- 검증: approved_with_notes (reports/validation/2026-08-31-saved-empty.md) — 빈 상태 일러스트 LCP priority 미적용 힌트 1건
- superseded_by: -
- 원본: 1f4d20f sdd/2026-08-31-saved-empty.md, tasks/2026-08-31-saved-empty.md
