# Entities

여러 feature에서 공유되는 핵심 도메인 모델이 있을 때만 사용한다.

## 사용 기준
- 여러 feature가 같은 도메인 개념을 공유한다.
- 해당 모델이 특정 feature에 종속되지 않는다.
- 타입, 식별자, 순수 규칙처럼 안정적인 개념이다.

## 규칙
- feature 전용 상태나 UI 로직을 두지 않는다.
- feature를 import하지 않는다.
- 필요해지기 전까지 새 entity를 만들지 않는다.
